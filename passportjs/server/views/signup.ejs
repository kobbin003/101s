<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0"
		/>
		<title>Document</title>
		<link
			rel="stylesheet"
			href="/css/authPage.css"
		/>
	</head>
	<body>
		<h1><%= formName %></h1>
		<% if (messages.error) { %> <%= messages.error %> <% } %>
		<form
			action="/register"
			method="post"
		>
			<% inputs.forEach(element => { %>
			<label for="<%= element %>"><%= element %></label>
			<input
				type="<%= element %>"
				name="<%= element %>"
				id="<%= element %>"
				required
			/>
			<% }) %>
			<button type="submit"><%= formName %></button>
		</form>
	</body>
</html>
